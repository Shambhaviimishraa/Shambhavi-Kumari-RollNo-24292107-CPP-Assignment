#include <iostream>
using namespace std;

int main() 
{
    int a, b, c;

    cout << "Enter value of a and b: ";
    cin >> a >> b;

    c = (a * a) + (b * b);

    cout << "Value of the expression is: " << c << endl;

    return 0;
}
